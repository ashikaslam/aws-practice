server {
    listen 80;
    server_name your-ec2-public-ip your-domain.com;

    location / {
        include proxy_params;
        proxy_pass http://unix:/home/ubuntu/aws-practice/gunicorn.sock;
    }
}
